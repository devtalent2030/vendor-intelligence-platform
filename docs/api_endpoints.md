# API Endpoints (FastAPI)

Planned endpoints (placeholder):
- `POST /vendors/` create vendor
- `POST /communications/` add email/log entry
- `GET /vendors/{id}/health` health score + risk level
- `POST /generate-report/{vendor_id}` export executive PDF summary
